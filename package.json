{
  "name": "parastruct",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
